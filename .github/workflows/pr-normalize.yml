# Source: jclee941/.github/.github/workflows/_pr-normalize.yml
name: PR Normalize
on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
permissions:
  contents: read
  pull-requests: write
jobs:
  normalize:
    uses: jclee941/.github/.github/workflows/_pr-normalize.yml@master
