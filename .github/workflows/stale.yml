# Source: jclee941/.github/.github/workflows/_stale.yml
name: Stale
on:
  schedule:
    - cron: '17 3 * * *'
  workflow_dispatch:
permissions:
  contents: read
  issues: write
  pull-requests: write
jobs:
  stale:
    uses: jclee941/.github/.github/workflows/_stale.yml@master
