# Source: jclee941/.github/.github/workflows/_pr-size.yml
name: PR Size
on:
  pull_request_target:
    types: [opened, synchronize, reopened]
permissions:
  contents: read
  pull-requests: write
jobs:
  size:
    uses: jclee941/.github/.github/workflows/_pr-size.yml@master
